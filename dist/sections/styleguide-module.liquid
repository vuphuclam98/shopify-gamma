{%- render 'styleguide-heading', heading: section.settings.title -%}
{%- render 'styleguide-module' -%}

{% schema %}
{
  "name": "Styleguide Modules",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Modules"
    }
  ]
}
{% endschema %}
