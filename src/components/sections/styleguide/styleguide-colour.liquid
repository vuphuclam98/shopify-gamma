{%- render 'styleguide-heading', heading: section.settings.title -%}
{%- render 'styleguide-colour' -%}

{% schema %}
{
  "name": "Styleguide Colours",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Colours"
    }
  ]
}
{% endschema %}
