<cart-page class="block min-h-[200px]">
  {%- render 'cart-main' -%}
</cart-page>

{%- capture src_url -%}
  {{- 'section-cart-page.js' | asset_url -}}
{%- endcapture -%}

{%- render 'atom_script', src: src_url, priority: 'important', type: 'defer' -%}
